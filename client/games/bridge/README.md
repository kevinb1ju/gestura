# GesturaGames — Bridge

Small web game using hand-tracking and gesture controls.

Getting started

1. Install deps

```bash
npm install
```

2. Run dev server

```bash
npm run dev
```

Notes

- This repo uses Vite + React + Tailwind.
- Add local environment variables to a `.env` file if needed.
